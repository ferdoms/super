import { WorkoutInterface } from '@interfaces/workoutInterface';
// import { Model } from '@models/model';

export class WorkoutModel {
    key = 'workouts';
    constructor(){
        
    }
    // save(wkt:WorkoutInterface){
    //     // wkt.id = this.autoIncrement();
    //     wkt = {id: this.autoIncrement(), name:wkt.name};
    //     this.items.push(wkt);
    //     this.storage.setItem(this.key, this.items);
    // }
    // update(id:number, wkt:wktInterface){

    // }
    
}